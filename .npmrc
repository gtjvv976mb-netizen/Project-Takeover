# anchor-bankrun (test-only) still declares a peer range of @coral-xyz/anchor ^0.30,
# but the client must match the program's 0.31.1 IDL format. The combination works —
# the 21 program tests pass against it — so allow the stale peer range rather than
# downgrade the client and break IDL parsing.
legacy-peer-deps=true
